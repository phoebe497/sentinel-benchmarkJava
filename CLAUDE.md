# Working context

Read `AGENTS.md` first. The project evaluates scanners against the first 100 OWASP BenchmarkJava test cases, then exposes normalized observations through a small Streamlit knowledge-search UI. Reports are short human summaries; raw evidence belongs under `artifacts/`.
